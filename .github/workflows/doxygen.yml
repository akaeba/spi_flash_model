# ************************************************************************
#  @author:     Andreas Kaeberlein
#  @copyright:  Copyright 2022
#  @credits:    AKAE
#
#  @license:    BSDv3
#  @maintainer: Andreas Kaeberlein
#  @email:      andreas.kaeberlein@web.de
#
#  @file:       doxygen.yml
#  @date:       2022-12-21
#
#  @see:        https://yonatankra.com/2-ways-to-use-your-docker-image-in-github-actions/
#  @see:        https://docs.github.com/en/actions/using-workflows/storing-workflow-data-as-artifacts
#  @see:        https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#patterns-to-match-branches-and-tags
#  @brief:      Runs Doxygen and pdf build
#
# ************************************************************************


name: doxygen

on:
  push:
    tags:
      - '**'    # '**'  Matches all branch and tag names.
    branches:
      - main
    pull_request:
      branches:
      - main

jobs:
  doxygen:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v6
      - name: Prepare Dirs
        run: |
          mkdir -p ./doxygen/out/latex
      - name: Run Doxygen
        uses: mattnotmitt/doxygen-action@v1.12.0    # https://github.com/mattnotmitt/doxygen-action
        with:
          working-directory: '.'                # Path of the working directory to change to before running doxygen
          doxyfile-path: './doxygen/Doxyfile'   # Path of the Doxyfile relative to the working directory
          enable-latex: true                    # Flag to enable make-ing of the LaTeX part of the doxygen output
      - name: Rename
        run: |
          mv ./doxygen/out/latex/refman.pdf ./${{github.event.repository.name}}_ug-${{github.ref_name}}.pdf    # move to top
      - name: Upload Artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{github.event.repository.name}}_ug-${{github.ref_name}}
          path: ${{github.event.repository.name}}_ug-${{github.ref_name}}.pdf
          retention-days: 30
